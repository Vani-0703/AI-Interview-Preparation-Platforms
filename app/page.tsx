'use client'
import {useState} from 'react'
const questions=['Tell me about yourself.','Why do you want this role?','Describe a challenging project you worked on.','What are your greatest strengths?']
export default function Home(){const [q,setQ]=useState(0);const [answer,setAnswer]=useState('');const [score,setScore]=useState<number|null>(null);function evaluate(){const words=answer.trim().split(/\s+/).filter(Boolean).length;setScore(Math.min(95,Math.max(35,45+Math.min(50,words))))}return <main className="wrap"><nav className="nav"><div className="brand">Interview<span>AI</span></div><div className="navlinks"><span>Practice</span><span>Resume Analyzer</span><span>Progress</span></div><button className="btn alt">Sign in</button></nav><section className="hero"><span className="pill">✦ AI-powered interview preparation</span><h1>Walk into your interview <span className="gradient">confident.</span></h1><p>Practice realistic interviews, get instant feedback, and turn every answer into a stronger one.</p><div className="actions"><button className="btn" onClick={()=>document.getElementById('practice')?.scrollIntoView({behavior:'smooth'})}>Start mock interview →</button><button className="btn alt">Analyze my resume</button></div></section><section className="grid"><article className="card"><div className="icon">🎙️</div><h3>AI Mock Interviews</h3><p>Practice behavioral, technical, HR, and role-specific questions in a realistic interview flow.</p></article><article className="card"><div className="icon">📄</div><h3>Resume Analyzer</h3><p>Discover missing skills, improve keywords, and understand how your resume performs for a role.</p></article><article className="card"><div className="icon">📈</div><h3>Instant Feedback</h3><p>Get structured feedback on clarity, confidence, relevance, and answer quality.</p></article></section><section id="practice" className="demo"><div className="card"><span className="pill">Question {q+1} / {questions.length}</span><h2>{questions[q]}</h2><textarea className="field" rows={8} value={answer} onChange={e=>setAnswer(e.target.value)} placeholder="Type your answer here..."/><div className="actions"><button className="btn" onClick={evaluate}>Evaluate answer</button><button className="btn alt" onClick={()=>{setQ((q+1)%questions.length);setAnswer('');setScore(null)}}>Next question</button></div></div><div className="card"><div className="small">LIVE FEEDBACK</div>{score===null?<><h2>Your interview coach is ready.</h2><p className="small">Answer the question on the left and get an instant practice score. This demo works without an API key; connect your preferred AI provider later for generative feedback.</p><div className="score">—</div></>:<><h2>Practice score</h2><div className="score">{score}<span className="small"> / 100</span></div><p className="small">Good start. Add a specific example, measurable impact, and a concise takeaway to make this answer stronger.</p></>}</div></section><footer className="small" style={{textAlign:'center',padding:'55px 0 20px'}}>© 2026 InterviewAI · Built for better interviews.</footer></main>}
